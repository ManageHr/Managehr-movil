<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
		  xmlns:tools="http://schemas.android.com/tools"
		  package="com.example.appinterface">

	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

	<application
			android:allowBackup="true"
			android:networkSecurityConfig="@xml/network_security_config"
			android:usesCleartextTraffic="true"
			android:dataExtractionRules="@xml/data_extraction_rules"
			android:fullBackupContent="@xml/backup_rules"
			android:label="@string/app_name"
			android:supportsRtl="true"
			android:theme="@style/Theme.AppInterface"
			tools:targetApi="31">

		<!-- Pantalla de inicio: SOLO el login tiene el LAUNCHER -->
		<activity
				android:name=".LoginActivity"
				android:exported="true">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>

		<!-- Pantallas con drawer: heredan de BaseActivity -->
		<activity
				android:name=".HorasExtraActivity"
				android:exported="true" />
		<activity
				android:name=".EstudiosActivity"
				android:exported="true" />
		<activity
				android:name=".ExperienciaActivity"
				android:exported="true" />
		<activity
				android:name=".HojasvidaActivity"
				android:exported="false" />

		<!-- Otras activities sin launcher... -->
		<!-- <activity android:name=".ProductosActivity" android:exported="true" /> -->

	</application>
</manifest>
